<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RelativeLayout
        android:id="@+id/headerLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/title_bar"
        android:orientation="horizontal"
        android:padding="@dimen/header_padding" >

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textColor="@android:color/white"
            android:textSize="@dimen/header_text_size"
            android:textStyle="bold" />
    </RelativeLayout>

    <ImageView
        android:id="@+id/touchImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/headerLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/margin_top_image"
        android:contentDescription="@string/touch_image_description"
        android:onClick="imageTouch"
        android:src="@drawable/touch" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/touchImage"
        android:layout_centerInParent="true"
        android:layout_marginTop="@dimen/margin_top_textView"
        android:text="@string/screen_1_textView"
        android:textColor="@android:color/black"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/logoImage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/logo"
        android:layout_marginBottom="@dimen/margin_bottom_logo"
        android:layout_marginRight="@dimen/margin_right_logo"
        android:contentDescription="@string/logo_description" />

</RelativeLayout>